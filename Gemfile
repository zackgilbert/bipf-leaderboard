# Gemfile
source 'https://rubygems.org'
gem 'sinatra'                 # web framework
gem 'sinatra-contrib'         # for cookie support
gem 'puma'                    # web server
gem 'pg'                      # database (Postgresql)
gem 'rake'                    # so we can run Rake tasks
gem 'sinatra-activerecord'    # for Active Record models
gem 'dotenv'                  # securely stored environment variables
gem 'oauth'                   # for twitter authentication
gem 'twitter'                 # looking up twitter posts
gem 'linkedin'                # for authentication + looking up linkedin posts
gem 'httparty'                # for authentication for linkedin
